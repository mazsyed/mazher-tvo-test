{
  "name": "mazher-tvo-test",
  "version": "1.0.0",
  "description": "Cypress test by Mazher",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cy-run": "npx cypress run --spec 'cypress/e2e/*' --browser chrome",
    "cy-run-crossbrowser": "npx cypress run --spec 'cypress/e2e/*' --browser chrome,edge,firefox",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "author": "Mazher Syed",
  "license": "ISC"
  
}
